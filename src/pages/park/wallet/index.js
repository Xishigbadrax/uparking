import ProfileLayout from "@components/layouts/ProfileLayout";

const Wallet = () => {

    return (
        <ProfileLayout>
            Wallet
        </ProfileLayout>
    );
}

export default Wallet;