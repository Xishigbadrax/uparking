import ProfileLayout from '@components/layouts/ProfileLayout';

const Notification = () => {
  return (
    <ProfileLayout>
            Notification
    </ProfileLayout>
  );
};

export default Notification;
