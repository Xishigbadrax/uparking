import ProfileLayout from "@components/layouts/ProfileLayout";

const Settings = () => {

    return (
        <ProfileLayout>
            Settings
        </ProfileLayout>
    );
}

export default Settings;